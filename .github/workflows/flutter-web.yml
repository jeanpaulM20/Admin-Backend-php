name: Build & Deploy Flutter Web App

on:
  push:
    branches: [ main ]
    paths:
      - 'trainer-flutter/**'
      - '.github/workflows/flutter-web.yml'
      - 'CNAME'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.4'
          channel: 'stable'
          cache: true

      - name: Get dependencies
        working-directory: trainer-flutter
        run: flutter pub get

      - name: Build Web
        working-directory: trainer-flutter
        run: flutter build web --release --base-href /trainer/

      - name: Copy build output to trainer/
        run: |
          rm -rf trainer
          cp -r trainer-flutter/build/web trainer

      - name: Commit updated trainer-app/ to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add trainer/ CNAME
          git diff --cached --quiet || git commit -m "chore: rebuild Flutter web app [skip ci]"
          git push
