import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand olive from sihltraining.ch
  static const Color primary     = Color(0xFF636B2F);
  static const Color primaryLight = Color(0xFF7A8438);
  static const Color primaryDark = Color(0xFF4D5324);

  // Dark surfaces — slight blue tint for cohesion
  static const Color background  = Color(0xFF0D1117);
  static const Color surface     = Color(0xFF161D2E);
  static const Color surface2    = Color(0xFF1E2740);

  // Text
  static const Color text        = Color(0xFFE8EAF0);
  static const Color muted       = Color(0xFF8A9AB8);

  // Border
  static const Color border      = Color(0xFF2A3550);

  // Semantic
  static const Color green       = Color(0xFF34D399);
  static const Color red         = Color(0xFFF87171);
  static const Color orange      = Color(0xFFFFAA33);
  static const Color blue        = Color(0xFF60A5FA);

  // Neutral
  static const Color white       = Color(0xFFFFFFFF);
  static const Color black       = Color(0xFF000000);
  static const Color transparent = Colors.transparent;
}
