# Admin Backend PHP

This project is built on the Yii framework. It now relies on the PHP OpenSSL extension for cryptographic operations.

Ensure that the OpenSSL extension is enabled in your PHP configuration to use features such as data encryption provided by `CSecurityManager`.

